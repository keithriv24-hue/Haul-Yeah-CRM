# PRD — Haul Yeah Moving CRM

## Original problem statement
Internal CRM for Haul Yeah Moving (weekend moving company, North/Central NJ). Owner: Keith Rivera II.
Stack: React + FastAPI + MongoDB (RBAC/users/time/notifications) + Airtable (CRM of record: Leads, Contacts, Projects, Tasks) + Square (live payments) + OpenPhone (SMS) + Emergent object storage (photos).
Roles: owner, sales, marketing, crew (legacy "employee" = crew). Brand: navy #1B2A4A / orange #E8743B.
Revenue calculations strictly use paid Square invoices. Crew/marketing must never see money data (quotes, commissions, costs, margins).

## Key architecture
- `/app/backend/server.py` — single FastAPI app (~4500 lines): Airtable proxy, auth (JWT, users in Mongo), Square sync, quotes PDF, jobs/assignments, time clock, notifications, team module, commissions.
- `/app/frontend/src` — React + shadcn. `pages/`, `components/`, `components/team/`, `components/crew/`, `lib/api.js`.
- Login: username/email + password. Owner: **HaulYeahAdmin / HaulYeah2026!** (renamed 2026-07-21). New employees: default password `haulyeah123`, must change on first login.
- Ghost POV accounts (hidden everywhere): TestCrewAdmin / TestSalesAdmin / TestMarketingAdmin (HaulYeah2026!).
- Teams derived from roles array: crew team + sales team; dual-team supported (e.g. Javante crew+sales). Multi-role users switch views via role switcher.

## Implemented (chronological)
### Earlier sessions (see CHANGELOG in git)
Leads pipeline w/ response timers, quote calculator (locked pricing v1.0: $65/man-hr, 10% cushion, $125/$75 travel, $85/stair, piano 500/800, 25% deposit), quote PDF, Square invoice sync (live), jobs board, crew time clock + GPS map + payroll report, projects, contacts, marketing module (funnel/ad spend/margin), tasks + blog with audience sharing, search bars everywhere, badges/notifications, user management (delete deactivated, default password), ghost accounts.

### 2026-07-21 — Team Profiles + Badges + Challenges (Phase 1 of engagement spec) ✅ tested iter_10 (100%)
- `/team` directory (all roles), `/team/:id` profiles (photo upload to object storage, display name, nickname, bio, role title, favorite move, fun fact; self-edit + owner moderation).
- Stats privacy: numbers (job counts, progress) ONLY self + owner. Others see locked/unlocked badges only.
- Badge tracks: crew (18 seeded) + sales (9 seeded), rarity bronze/silver/gold medallions, auto-unlock from job credits, owner-awarded ones, owner can create/edit/deactivate badges. Pin up to 3. Rookie empty state.
- Job credits (source of truth, everyone starts at 0): owner manual credit (Team HQ) + approval prompts auto-generated when crew completes a job (role-tagged Driver/Helper) or a sales member books a lead. Badge-specific hint messages. Prompts on owner Dashboard + Team HQ.
- Challenges: individual/competition, job_credits (auto) or owner_verified metric, team-scoped visibility (dual sees both; marketing views both; owner all), rewards = badge or profile title, auto-finalize at end date, owner confirm for verified. 4 seeded.
- `/leaderboard`: Crew (jobs) + Sales (closes) monthly tabs, counts only; Hall of Fame permanent monthly archive.
- `/team-admin` (Team HQ, owner): Credits / Badges / Challenges / Crew insight (Driver vs Helper comparison) / Log (audit).
- Invite: "Invite" copy button on Team tab rows (copyable text w/ login + default password).
- Nav bell now for all roles.

### 2026-07-21 — Features 1–3 spec ✅ tested iter_11 (100%)
- **F1 Commissions**: `lead_commissions` Mongo docs keyed by Airtable lead id (Airtable stays source of truth for jobs; no second job entry surface). Attribution (quote_sent_by stat-only, closed_by earns, move_type, quote_amount, job_date) editable by sales+owner on Lead detail (CommissionCard). Payment flags (deposit_paid/fully_paid/refunded) owner-only, auto-flip from Square invoice sync (deposit → pending; full paid → locked). Engine: <$1500 flat by move type (25/40/40/60/60/60), $1500–2999 = 10%, $3000+ = 12% — owner-editable rate table. Statuses: pending → locked, refunded = voided (removed from totals). `/commissions` page (owner all reps / sales own only) with day/week/month grouping + drill-down + rate editor. Crew/marketing: 403 + no nav.
- **F2 Availability**: Crew page → Availability tab (owner): month day-grid, Sat/Sun highlighted, off vs available per day, shortage flag (available < crew needed from that day's assignments, min 2/job), short-weekend banner, owner can toggle members off/on per day. Crew still self-serve via Days Off.
- **F3 Script**: verbatim call script (Opening/Scope/Access/Special Items/Distance/Timing/Close + surcharge checklist v1.0 + red flags + no-address response) as collapsible panel side-by-side with Quote Calculator, plus standalone /script page. Owner + sales only.
- **Profile first-task**: every user has `profile_task`; open → pinned orange card at top of To-Do linking to own profile; auto-done on profile save; new users get it automatically + bell notification.
- Owner login renamed HaulYeahOwner → **HaulYeahAdmin** (startup migration; OWNER_EMAIL in backend/.env). Owner account listed/editable in Team tab; guard against removing own owner role; usernames without "@" now allowed on edit.

## Notification Center spec (user-approved phasing; ask permission before each phase)
- **Phase 2 ✅ DONE (tested iter_12, 100%)**: `/notifications` page (owner/sales/marketing; crew 403 + no route): All/Email/Social/Alerts tabs; new-lead alerts with live uncontacted timer (green <5m, orange ≥5m, red+pulse ≥15m, green "Contacted in Xm" once lead_meta.contacted_at set); booked alerts auto-created on status→Booked; Zapier inbound webhook POST /api/webhooks/zapier?token=SECRET (token in settings doc `zapier_webhook`, owner setup card w/ copy URL; fields kind/title/body/lead_name/external_id dedupe); background lead_alert_loop polls Airtable (24h window, 2-min interval) when key present; per-principal read markers; nav unread badge auto-clears on visit. Email tab placeholders: contact@ (all 3 roles), keithrivera@ (owner-only render). Social tab owner+marketing only (FB/IG placeholders).
- **Phase 3 ✅ BUILT (tested iter_13, 100%)**: Gmail via Google OAuth. Backend: /api/gmail/status|connect/{id}|disconnect/{id}, public /api/gmail/oauth/callback, /api/gmail/{id}/messages (list/read/reply/archive/unread/search), tokens Fernet-encrypted (key from JWT_SECRET) in `gmail_tokens`; unread counts feed nav badge. UI: GmailCard on Settings (owner) w/ setup instructions + redirect-URI copy + Connect buttons (disabled until GOOGLE_CLIENT_ID/SECRET in backend/.env); EmailInbox two-pane wired into Notifications Email tab (connected boxes → inbox, else placeholders + owner setup link). contact@ = owner+sales+marketing; keithrivera@ = owner-only (404 for others). AWAITING user's Google Cloud OAuth Client ID/Secret.
- **Phase 4 ✅ BUILT (tested iter_13, 100%)**: Meta FB/IG social feed, owner+marketing only. Backend: /api/meta/status|connect|disconnect|feed|refresh, public /api/meta/oauth/callback (long-lived page tokens + linked IG account, Fernet-encrypted in `meta_tokens`); meta_poll_loop (5-min) pulls FB post comments, Messenger+IG conversations, IG comments + mentions into `social_items` (dedupe _ids). UI: MetaCard on Settings (owner) w/ Meta app setup instructions (Dev Mode is fine — owner is app+page admin, no App Review needed); SocialFeed in Notifications Social tab w/ refresh + "Reply on Facebook/Instagram" deep-links. AWAITING user's META_APP_ID/SECRET.
- Visibility matrix in original spec (Section 5) is the contract; enforce server-side.

## Backlog
- P0: User owes credentials → GOOGLE_CLIENT_ID/GOOGLE_CLIENT_SECRET + META_APP_ID/META_APP_SECRET (backend/.env; instructions live on Settings page cards). Once provided: connect flows unlock; then live-test inbox + social feed.
- P1: Notification dropdown preview (tap badge → newest shared task/blog titles).
- P2: Task comments; Lead source backfill from lead detail.
- Optional: mirror commission fields to Airtable columns if user adds them (currently app-side by design, user chose option a).
- User instructions owed when Phases 3/4 built: Google OAuth + Meta app setup guides.

## Environment notes
- Preview lacks AIRTABLE_API_KEY (amber banner expected). Square is LIVE — never send invoices in tests.
- Deployed app: haulyeahadmin.com (user deploys via Emergent Deploy; remind about env vars OWNER_EMAIL there is not needed — code default is haulyeahadmin, migration renames legacy owner emails at startup).

- **Phase F (2026-07-26)** — AI Ops panel done. Remove "Phase F" from remaining list; Phase E (routes) still open.

## Test reports
iteration_1..9 (earlier modules) · iteration_10 (team module, 100%) · iteration_11 (commissions/availability/script/profile-task, 100%) · iteration_12 (alerts/zapier, 100%) · iteration_13 (gmail wiring + meta phase 4, 100%) · iteration_14 (dispatch board, 100%) · iteration_15 (checklists/timeline/username-login/unscheduled-flag, 100%) · iteration_16 (fleet management, 100%) · iteration_17 (customer portal, 18/18 after INTEGRATION_PLAIN fix). Pytest suites in /app/backend/tests/: test_commissions_availability_profiletask.py, test_iteration12_alerts_webhooks.py, test_iteration13_gmail_meta.py, test_iteration14_dispatch.py, test_iteration15_checklists_timeline.py, test_iteration16_fleet.py, test_iteration17_portal.py (dates now dynamic via conftest.py; run with default -n 2).

## CRM Extension project (approved 2026-07-21, user rules: extend don't replace, pause after each phase)
User choices: phase order approved · route optimization = free OSRM/OpenStreetMap (give setup instructions if needed) · AI dashboard = Emergent LLM key (GPT) · tipping = Square payment link w/ tipper name field · pause for review between phases.
- **Phase A ✅ DONE (iter_14, 100%)** — Dispatch Board `/dispatch` (owner nav between Crew and Jobs). ONE new endpoint GET /api/dispatch/board?date= (assignments for ET day + behind flag [arrival+15min & status Assigned/En Route] + clocked_in count per job + crew pool w/ jobs_today/jobs_week/clocked_in/off + truck pool w/ on_job + revenue_today from paid Square invoices ET-day + counts). All mutations REUSE PATCH /api/assignments/{id} incl. 409 conflict→"Assign anyway" dialog. Native HTML5 drag-drop (crew chips pool→job, job→job two-step move, truck chips), click position badge flips Driver/Helper, x removes, reuses AssignmentModal for create/edit. todayET() matches backend _et_today(). 2 QA demo assignments left on 2026-07-21 (Montclair + Hoboken).
- **Phase B ✅ DONE (iter_15, 100%)** — Operations Timeline + 5 Moving Checklists. `job_events` collection + log_job_event hooks in create/update_assignment (crew added/removed, truck changes). GET /api/assignments/{id}/timeline = computed merge (created_at, status_history, job_events, time_entries clock in/out, job_photos) desc-sorted; RBAC owner or crew-on-job. CHECKLIST_TEMPLATES (warehouse_departure/arrival/loading/delivery/completion, 5 items each, 25 total) stored per-assignment in `job_checklists`; GET .../checklists + POST .../checklists/{key}/items/{idx}; completing a list auto-advances exec_status (warehouse→En Route, arrival→Arrived, loading→In Progress, completion→Complete; delivery none; NEVER backwards) via _apply_exec_status (extracted from set_job_status, keeps Complete side-effects: review prompt, credit prompts, timelog sync). UI: JobChecklists.jsx + JobTimeline.jsx; crew My Jobs card gets "Checklists" toggle; Dispatch cards get progress ring (checklist_done/25 from board endpoint) + "Timeline" button → dialog w/ Timeline+Checklists tabs.
- **Also DONE (iter_15)**: (a) new accounts accept plain USERNAME or email as login (create_user: 3+ chars, no spaces; Team tab dialog relabeled); (b) unscheduled clock-ins flagged: _todays_assignment now uses ET date, flag 'not_scheduled_today' + owner bell notification ("Unscheduled clock-in") + amber "On clock, no job" badge in Dispatch crew pool.
- **Phase C ✅ DONE (iter_16, 100%; 37/37 pytest + full UI)** — Fleet Management extends existing `trucks`. New page `/fleet` (owner nav, Wrench icon). Extended PATCH /api/trucks/{id}: mileage, fleet_status (in_service/needs_attention/in_shop), registration{number,expires}, insurance{carrier,policy,expires}, reminders[]. New collections: truck_inspections (8-item daily check; fail → needs_attention + owner flag notify; odometer bumps mileage), truck_logs (maintenance+fuel w/ totals), truck_damage (crew reports → owner notify, resolve/reopen), truck_photos (object storage; serve /api/truck-photos/{id}?auth=). GET /api/fleet aggregates all + expiry states (soon ≤30d/expired). USER-REQUESTED INTEGRATION: checking "Truck inspected" (warehouse_departure item 0) auto-opens InspectionDialog; inspection with assignment_id auto-marks that item (_maybe_complete_checklist helper) + timeline event. Crew get "Daily inspection" button on job cards. UI: Fleet.jsx, fleet/TruckDetailDialog.jsx (4 tabs), fleet/InspectionDialog.jsx. Demo: Truck 2 (open damage, failed tire inspection, reminder due, reg exp soon), Truck 3 (TEST-prefixed values).
- **Phase D ✅ DONE (iter_17, 18/18 backend + full UI after 1-line INTEGRATION_PLAIN fix)** — Customer Portal expands public /track/{token} (no login). Extended GET /api/track/{token}: countdown data, status (derived from matching assignment exec_status by job_date+crew, else Crew assigned/Scheduled), crew+truck, quote/deposit/remaining_balance/paid_in_full, invoices (square public_url), saved details, uploads, review_submitted, tips_enabled, review_link (business.reviewLink). Public POST endpoints: /details (gate_code/elevator/parking/special_requests/inventory_notes → job.portal_details), /uploads?kind=photo|inventory (image/PDF, 15MB, 30/job cap, object storage, 15-min throttled owner ping), /tip (REAL Square payment link via /v2/online-checkout/payment-links, $1–$1000, tipper name, portal_tips collection), /review (1–5 stars, ≥4 returns Google link). _owner_portal_ping = bell notification (kind 'portal') + SMS via OpenPhone to NEW integrations field owner_alert_phone (IntegrationsCard input 'Your cell for portal alerts'; empty = no SMS). Owner: Jobs board PortalInfo block + GET /api/jobs/{id}/portal-uploads. UI: Track.jsx rewritten (dark theme kept; en-route banner gated to pings ≤120min); components/portal/PortalSections.jsx. Demo: job QA-1001 token afe748c700fc481497a5720e5d80acde (Dana Johnson, details+photo+5★ review+$2 tip link seeded).
- **Phase F ✅ DONE (self-tested e2e: facts, LLM brief, 30-min cache, 403 gates, UI panel)** — AI Operations Dashboard. GET /api/ai/ops-brief (owner-only): _ops_facts() gathers Mongo-only snapshot (today's jobs w/ behind + late-risk [arrival ≤60min & nobody clocked in], revenue today, crews clocked in + unscheduled, fleet flags [needs_attention/in_shop/reminders due/reg-ins expiry/open damage], outstanding balances from jobs deposit-paid-not-full, schedule conflicts [double-booked crew today+tomorrow, scheduled-but-OFF], overtime >8h/day, customer issues [≤3★ portal reviews 14d + open damage]). Brief written by gpt-5.4 via emergentintegrations LlmChat + EMERGENT_LLM_KEY ('DO:' prefix on action items), cached 30 min in ai_briefs, ?refresh=1 forces regen, LLM failure falls back to cached text + raw facts. UI: components/AiOpsPanel.jsx (dark navy 'Ops brain' panel at top of owner Dashboard: clickable metric chips deep-linking to /dispatch /fleet /jobs, brief lines w/ orange DO: highlighting, conflict lines, Refresh btn).
- **Phase E (REMAINING, user skipped ahead)** — Route Optimization: reuse geocode() (Nominatim) + OSRM public API for drive times/mileage; suggested stop order, fuel estimates, multi-stop (storage/donation/warehouse returns), live-traffic placeholder. Optional add-on: auto-text customer an ETA when crew starts driving.
- **Phase F** — AI Operations Dashboard: panel on existing owner Dashboard via Emergent LLM key (today's jobs/revenue/crews, maintenance due, outstanding balances, overdue follow-ups, conflicts, overtime + late predictions, upcoming estimates). Assistant-style, not a chatbot.

## Code Quality & Test Suite Overhaul (2026-07-26, iter_18, 342/342 pytest + 16/16 regression)
- Fixed date-drift permanently: new /app/backend/tests/conftest.py loads .env for ALL test modules and reseeds QA data every run (QA Dispatch Montclair/Hoboken → today ET, resets Hoboken to Assigned/no crew/no truck, track job QA-1001 → today+3, deletes leftover TEST_/TEMP/QA Prompt assignments, prunes test-artifact portal uploads vs 30-file cap). Tests now compute TODAY_ET/QA_DAY/TRACK_JOB_DATE dynamically — NEVER hardcode dates in tests.
- Fixed stale owner username: 6 older test files still logged in as pre-rename "HaulYeahOwner" → repeated 401s tripped the 5-try brute-force lockout (keyed ip+username) and cascaded 429s across the whole suite. All now import OWNER_EMAIL from test_config.
- Fixed stale RBAC expectations in test_auth.py (sales/crew legitimately have audience-filtered tasks+blog since iter9) and /auth/me shape (extra user key).
- Server fix: clock-in now honors PunchPayload.assignment_id (was silently ignored; punch attached to whichever today-assignment Mongo returned first).
- Refactor (behavior-preserving, all requested hotspots): login → _login_user_account/_login_shared_password; send_square_invoice → _square_customer_body/_square_order_lines/_publish_square_invoice/_record_square_invoice; ensure_job_for_deposit → _sync_existing_deposit_job/_job_doc_from_invoice; apply_invoice_to_jobs → _alert_payment_landed/_mark_job_paid_in_full/_mark_job_payment_pending; handle_square_event → _handle_invoice_event/_update_known_invoice_from_webhook/_invoice_doc_from_webhook; patch_job → _resolve_job_crew/_job_updates_from_payload/_notify_new_crew_members; portal_upload → _validate_portal_upload/_store_portal_upload/_notify_portal_upload. Return type hints added to payment/invoice/portal endpoints. pyflakes clean (no undefined vars).
- crew_module QA fixture moved to tomorrow ET + ignore_warnings (avoids colliding with QA Dispatch seed now pinned to today).
- Regression: testing agent iteration_18 → 16/16 pass, zero issues.

## 2026-08-04 — Checklist clock-in gate + Meta CAPI env config
- Crew can no longer work job checklists until clocked in: POST /api/assignments/{id}/checklists/{key}/items/{idx} returns 403 ("Clock in first — checklists unlock once you're on the clock.") for non-owner roles with no open time entry; owner exempt (Dispatch unaffected). UI: CrewJobs fetches /api/crew/my-time clocked_in, JobChecklists gets clockedIn prop → amber notice (data-testid checklists-clock-in-notice) + disabled checkboxes. New pytest TestChecklistClockInGate in test_iteration15 (Junior, temp assignment); iter16 inspection test now unchecks as owner. 61/61 iter15+16 pass; UI verified via screenshot as Javante.
- Meta Conversions API (CAPI) env plumbing added, config only, no behavior change: backend/.env now has META_DATASET_ID, META_CAPI_ACCESS_TOKEN, META_TEST_EVENT_CODE (empty — user pastes real values in secrets panel; META_APP_ID/META_APP_SECRET already existed). server.py: meta_capi_config()/meta_capi_configured() read os.environ; startup logs set/unset state for all 5 vars (verified in logs). No CAPI event-sending code yet — awaiting user values + future task.

## 2026-08-04 — Meta CAPI Lead + Purchase events (backend + Settings status row)
- New CAPI module in server.py (near meta_capi_config): _capi_hash/_capi_user_data (Meta-normalized SHA-256 email/phone/external_id, raw PII never sent), _capi_post (graph.facebook.com/v26.0/{META_DATASET_ID}/events with access_token + appsecret_proof HMAC + optional test_event_code), queue_capi_event (outbox in `meta_capi_events` collection, one doc per event_id, background delivery via asyncio task), _capi_deliver (sent/failed + attempts + error), capi_retry_loop (every 5 min, retries pending/failed, max 5 attempts). Silently no-ops while META_DATASET_ID/META_CAPI_ACCESS_TOKEN are unset.
- Hooks: (1) lead_alert_loop Airtable poll → "Lead" event event_id lead:{rec_id} with hashed lead email/phone; (2) Zapier webhook kind=new_lead → "Lead" with hashed external_id; (3) apply_invoice_to_jobs PAID deposit/full → "Purchase" event_id deposit:{invoice_id} with currency USD + amount + hashed customer email/phone. action_source system_generated.
- GET /api/meta/capi-status (owner-only): configured flags per var + sent/pending/failed counts + last_sent_at/last_error. Settings → Facebook & Instagram card now shows a "Conversions API (ad signals)" row (data-testid meta-capi-status): Off + setup hint, or On + counts + test-mode warning.
- Tested: unit (hash normalization, empty-identifier no-op), full pipeline e2e with fake creds against real Graph API (reached Meta, 400 code 190 captured, attempts++, dedupe holds), pytest iteration12+13 (59/59), UI screenshot. Real delivery pending user pasting META_DATASET_ID + META_CAPI_ACCESS_TOKEN in secrets panel (+ prod redeploy).

## 2026-08-04 — CAPI refactored onto user-supplied backend/meta_capi.py + Schedule event
- Created /app/backend/meta_capi.py byte-for-byte from user's upload (verified diff). All CAPI payload building + sending now goes through it: build_user_data (em/ph/fn/ln/zp/external_id hashed, fbc/fbp/fbclid/IP/UA raw), send_event (graph v21.0 default, META_GRAPH_VERSION overridable, appsecret_proof, test_event_code, never raises).
- server.py keeps only the Mongo outbox around it: queue_capi_event(event_name, event_id, user_data, custom_data, action_source, event_source_url) → meta_capi_events doc → _capi_deliver via meta_capi.send_event ("skipped" result leaves doc pending without burning attempts) → capi_retry_loop. Removed old internal _capi_hash/_capi_user_data/_capi_post.
- Event IDs now follow the module's pixel-dedup convention: {lead_id}-Lead, {lead_id}-Schedule, {lead_id}-Purchase-{invoice_id}.
- Hooks: Airtable new lead → Lead (website); Zapier new_lead → Lead, Zapier booked → Schedule (phone_call); patch_job first crew assignment (crew empty→non-empty) → Schedule (phone_call); Square deposit/full PAID → Purchase (website, value+USD).
- Tested: pyflakes clean, e2e outbox-through-module with fake creds (error capture, attempts, skip path), full pytest 358/359 (1 known cross-worker flake, passes solo), capi-status endpoint live.

## 2026-08-04 — CAPI final wiring per user spec (A1-A4), direct fire-and-forget
- Architecture change: outbox/retry loop REMOVED per user spec. All Meta calls are now asyncio.create_task(_capi_log(label, meta_capi.fire_*(...))) — fire-and-forget, never raise into request path. _capi_log (near fetch_lead_details) awaits the fire, inserts a result row in meta_capi_events (status sent/failed, label, error; skipped = no row) so the Settings counters keep working. Old queue_capi_event/_capi_deliver/capi_retry_loop deleted.
- A1: POST /api/webhooks/tally (public_router, next to zapier webhook). Verifies Tally-Signature = base64(HMAC-SHA256(raw_body, TALLY_WEBHOOK_SIGNING_SECRET)); 401 on missing/bad sig or unset secret. Parses lowercased labels (full name, email/email (optional), phone, moving from (zip code), fbclid/fbc/fbp, event_source_url default https://haulyeahmoves.com/), captures x-forwarded-for IP + UA, fires fire_lead. TALLY_WEBHOOK_SIGNING_SECRET added to backend/.env (empty). NOTE: field label keys may need adjusting to the real Tally form question labels.
- A2: PATCH /api/tables/leads/{id} — when payload sets LEAD_STATUS_F (fldplIOmtbERJFG6X) to "Booked", reads previous status via Airtable GET first; fires fire_schedule only on the not-Booked→Booked edge (lead dict from fetch_lead_details, id=record id).
- A3: apply_invoice_to_jobs PAID branch fires fire_purchase(lead, value=amount, order_id=invoice_id) for EVERY paid invoice (deposit/full/balance); lead looked up via fetch_lead_details(lead_id), falls back to invoice customer fields (id=invoice_id if no lead). Meta dedups on {id}-Purchase-{invoice_id}. Superseded hooks removed: Airtable-poll Lead, zapier new_lead/booked, patch_job Schedule.
- A4: POST /api/marketing/capi-test (owner-only): {lead_id?} → fires Lead for that Airtable lead or a synthetic test lead; returns meta_capi's raw result ({ok, response} / {skipped}).
- Startup logs a WARNING when meta_capi.capi_enabled() is false.
- Tested: tally webhook valid sig 200 / bad + missing sig 401 (temp secret, then reset to ""), capi-test 200 skipped + 401 unauthenticated, purchase fire path e2e with fake creds (Meta 400 captured in log row, request path unaffected), skip path writes no rows, full pytest 358 passed (1 known cross-worker flake, passes solo).
- USER TODO: paste TALLY_WEBHOOK_SIGNING_SECRET (+ META_* CAPI vars) in secrets panel, point the Tally form webhook at /api/webhooks/tally, redeploy.

## 2026-08-05 — CAPI live-test fixes (FIX 1-4) after user verified on dataset 1913145279380964
- FIX 1 Schedule on Booked: update_record pre-reads LEAD_STATUS_F, fires meta_capi.fire_schedule only on not-Booked→Booked edge for the leads table; logs "CAPI Schedule firing for lead {id} (prev status: {s})". Verified e2e in-process with stubbed airtable_request + fake Meta creds: Quoted→Booked fires exactly once; Booked→Booked and Contacted patches do not.
- FIX 2 real amount: Purchase value = square_invoices doc amount, falls back to lead Quote field fldI5HufBfW35A1fD (verified 850 real / 1200 fallback).
- FIX 3 full match keys: new lead_dict_from_airtable(record) + fetch_lead_record(lead_id) helpers; LEAD_CAPI_F map added (fbc fld8uG29qAHynjk5S, fbp flddnX9dbcQhmELkJ, fbclid fldn7zMBVE1gyXNrp, event_source_url fldlN65ocmrgyks5Z; zip = Moving from fldHysDcz9GAos0zD). Used by Schedule, Purchase (invoice-customer fallback when no lead) and capi-test. Verified user_data carries em/ph/fn/ln/zp/fbc/fbp/external_id. _lead_capi_dict removed.
- FIX 4 fire once: capi_purchase_sent flag on square_invoices doc, gated on meta_capi.capi_enabled() so an unconfigured env never burns the flag; redelivery skipped (verified). Event id stays {lead_id}-Purchase-{invoice_id}.
- testing_agent iteration_19: 6/6 executable HTTP tests passed (capi-test skip/404/auth, tally 401, capi-status, login); Airtable HTTP flows skipped (AIRTABLE_API_KEY empty in preview — live only in prod); agent test file /app/backend/tests/test_iter19_capi_fixes.py auto-skips without the key and greps backend logs for the Schedule line when key present.
- Reviewer note (accepted, no change): pre-read fail-open could re-fire Schedule after transient Airtable 5xx — harmless because event_id {id}-Schedule is stable and Meta dedups.
- USER TODO: redeploy, then re-test in Events Manager (Quoted→Booked = 1 Schedule; pay invoice = 1 Purchase with real amount + match keys). fbc/fbp landing-page capture (haulyeahmoves.com + Tally hidden fields → Airtable) is OUTSIDE this codebase.

## 2026-08-05 — Quote calculator fixes (mileage verified, 6-hr floor, crew/hours → Project)
- Mileage verified correct & relabeled: $0.85 × max(0, miles − 20); line reads "Mileage — X mi beyond the first 20 × $0.85/mi"; travel fee line notes it covers first 20 miles. Constants confirmed live: $65/mh, 10% cushion, $125/$75 travel, $85/flight, piano 500/800, deposit 25%, rounding $50.
- 6-hour floor IN THE MATH: computeQuote now takes minHours, returns effective `hours` + `minHoursApplied`; Calculator + QuoteModal pass minHours=6 when lead homeSize is 3BR/4BR+; amber note data-testid min-hours-note when floored; crew charge uses effective hours.
- Cushion no longer a visible line item (line-cushion removed); still folded into finalQuote (Final > Subtotal). Customer-facing invoiceLineItems already folded it (unchanged).
- Book as job: Calculator + QuoteModal save {crew, hours: q.hours} into the lead_quotes breakdown (PUT /api/quotes/{lead_id}); leadActions.bookLeadAsJob now fetches the breakdown and maps crew → PF.crewSize (fldkeeMYBMooCqi7p), hours → PF.estHours (fldhVlH3Cu66W5EYo) on the created Project.
- testing_agent iteration_20: 12/12 pricing unit tests, backend breakdown round-trip, full UI checks — 100% pass, zero issues. New regression file /app/backend/tests/test_iter20_quote_calc.py. NOTE: lead-picker/3BR floor UI + actual booking need Airtable (prod-only) — verify there after redeploy.

## 2026-08-05 — Universal quote calculator consolidation
- ONE calculator component: /app/frontend/src/components/QuoteCalculator.jsx (also exports useLivePricing). Used by /calculator page (thin: lead picker + ScriptPanel + CrewGuideCard) and QuoteModal (thin Dialog wrapper used by Leads.jsx + LeadDetail.jsx). Help.jsx import updated. QtyStepper/packing input removed per spec (inputs are exactly: Home Size, Crew, Hours, Travel, Stairs, Elevator, Piano select, Miles — in that order).
- Home Size is now an in-calculator select (auto prefills crew/hours from PREFILL_BY_SIZE and drives the 6-hr floor on 3BR/4BR+); lead pick seeds it from LF.homeSize.
- Output is a RANGE: pricing.js computeQuote returns quoteLow=ceil(subtotal/50)*50 and quoteHigh=ceil((subtotal+10%)/50)*50; finalQuote===quoteHigh (backcompat for PDF/invoice); deposit=25% of high end. Cushion never rendered anywhere (also removed 'Cushion — 10%' bullet from scriptContent.js surcharge checklist).
- Elevator: informational yes/no, no charge, saved into breakdown + notes. Piano: select None/Upright($500)/Grand($800) via calc items (FALLBACK_PIANOS if API empty).
- Save writes: quoteSaveFields (quote=high, status Quoted, notes with range) + name-keyed lead columns via quoteStructuredFields {'Crew Size','Est Hours','Deposit Amount'} with graceful retry without them (toast if columns missing in Airtable). Breakdown now stores crew/hours/quoteLow/quoteHigh/deposit/elevator.
- testing_agent iteration_21: 100% frontend pass (owner + sales parity, prefill, floor, range $750–$800 case, piano, mileage, elevator, Help regression). PROD FOLLOW-UP: confirm Leads table has columns named exactly 'Crew Size', 'Est Hours', 'Deposit Amount' (else the fallback toast appears); e2e save test needs Airtable.

## 2026-08-05 — Quote save now maps by Airtable field ID (user-confirmed)
- quoteStructuredFields in lib/quote.js writes Crew Size → fldz0UbjzI0MADcnh (number), Est Hours → fld74xIlr9zV9PZzA (number), Deposit Amount → fldqYyz9kkI0RDbdr (currency). Rename-proof; fallback retry + toast kept as safety net. Verified compile + mapping present; e2e write needs prod Airtable.

## 2026-08-06 — Code review Step 1: secrets removed from test code (STOPPED after step 1 per user)
- All hardcoded credentials/tokens moved to /app/backend/tests/.env.test (gitignored — .gitignore already had .env + .env.* at lines 103-104); committed template tests/env.test.example; test_config.py now fail-fast _require() with NO inline defaults; legacy role passwords fall back to backend env (SALES_PASSWORD/EMPLOYEE_PASSWORD).
- Cleaned: test_config defaults, SellMoves2026! (auth/crew/todo_blog/iter9), CrewDay2026!, HaulCrew2026! (junior initial), TempPass→random uuid pw, owner/crew creds in iter18/iter19/iter20, QA track token afe748… (conftest/iter17/iter18), password leaked in comment.
- Bonus fix found while verifying: test_team_module TODAY/YESTERDAY used UTC vs server ET → challenge lifecycle failed after UTC midnight; now ET-based.
- Full suite: 365 passed, 5 skipped (Airtable-gated), 1 known cross-worker flake (passes solo).
- REMAINING code-review steps (user will trigger): Step 2 undefined-variable warnings, Step 3 React analyzer score 0, Step 4 complexity refactor (build_quote_pdf, apply_invoice_to_jobs, _job_doc_from_invoice, build_user_data, send_event), then is-vs-== cleanup / type hints / server.py split.

## 2026-08-08 — Code review Steps 2–4 complete (iter_22, 393/393 pytest, eslint 0 errors)
- Step 2 (undefined vars): initialized `payload` (decode_token, principal_from_token_string), `sleep_for` (RateLimiter.wait); meta_capi `ph`/`resp`/`data` handled via restructure. Pylint used-before-assignment: 10/10.
- Step 3 (React scan failure): root cause = ESLint 9 (flat-config-only) installed with NO eslint.config.js anywhere, so any scanner running eslint exited immediately. Created /app/frontend/eslint.config.js (flat config, react + react-hooks plugins, allowEmptyCatch, ui/ exempt from no-unknown-property) + `yarn lint` script. Result: 0 errors, 12 minor warnings.
- Step 4 (complexity): build_quote_pdf → 6 _pdf_* helpers + module color constants; apply_invoice_to_jobs → _maybe_fire_capi_purchase + _capi_lead_and_quote; _job_doc_from_invoice → _job_customer + _job_paid_in_full; meta_capi build_user_data → _hashed_user_fields + _raw_user_fields; send_event → _build_payload + _auth_params + _post_events. Behavior verified identical (27 targeted regression tests, iteration_22.json).
- Bonus fix: /api/assignments/{id}/timeline no longer drops the 'created' event when history >200 entries (insert after slice); conftest now prunes Montclair seed job_events/time_entries to stop data drift.

## 2026-08-08 — React "0/100" reviewer failure: second root cause fixed
- Root cause #1 (fixed earlier): ESLint 9 flat-config-only, no eslint.config.js → standalone lint crashed.
- Root cause #2 (fixed now): `CI=true yarn build` failed — CRA turns warnings into errors under CI; AppContext.jsx:141 had react-hooks/exhaustive-deps warning (missing `celebrateNewPaid` dep). Added the stable dep. CI build now exits 0 (443KB gzip bundle). eslint: 0 errors/11 minor warnings. Dashboard smoke OK.

## 2026-08-08 — Reviewer 0/100 FINAL reconciliation (fresh-clone install was impossible)
- Reviewer scans a fresh clone; frontend/yarn.lock had NEVER been committed in repo history → no lockfile in any clone.
- npm ci: instant fail (no package-lock). npm install: ERESOLVE hard fail (react-day-picker@8.10.1 peer date-fns ^2/^3 vs date-fns 4.1.0). Then CRA ajv hoisting bug (ajv-keywords@5 needs ajv@8, npm hoists ajv@6).
- Fixes (branch main): 93859a5 commit yarn.lock + frontend/.npmrc (legacy-peer-deps=true); d4cb777 ajv@^8.17.1 direct dep; fbaf895 npm-ci-valid package-lock.json.
- Verified on fresh clones: yarn frozen-lockfile+CI build=0, npm install+CI build=0, npm ci+CI build=0. Preview app healthy.
- NOTE: no git remote in pod — user must push via "Save to GitHub" for external reviewer to see fixes. Maintenance: if deps change via yarn, regenerate package-lock.json (npm install) to keep npm ci valid.

## 2026-08-08 — Removed real-person test accounts (user request: option b+d)
- Deleted Javante Brown + Junior Santil users (backup: memory/removed_users_backup.json); job/time-clock history kept.
- server.py startup: seeds removed + active delete_many on those emails (cleans production on next deploy).
- Test suite rewired: conftest seeds ephemeral "QA Crew One/Two" (qa.crew1/2@haulyeah.test, fixed old ids so QA fixtures keep working) and deletes them at session end (controller-only atexit). All JAVANTE_/JUNIOR_ constants renamed CREW1_/CREW2_ across tests + .env.test.
- Also removed leaked "QA Temp Crew" artifact and made test_create_reset_deactivate_user delete its temp user.
- Full suite: 393 passed. Roster after run: owner only. Old logins return 401.

## 2026-08-21 — 7-step redesign+two-tier-quoting project STARTED (pause after each step)
- Step 1 DONE: applied haul-yeah-crm-redesign.zip (95 files: 93 frontend + design_guidelines.json + CHANGES.md). Verified: zip contained no package.json/locks/.env/backend files; QuoteCalculator math byte-identical; all data-testids preserved (4 false alarms were testid-via-prop). CI build exit 0, eslint 0 errors (warnings 11→6). Testing agent iteration_23: 100% pass — 4 roles land correctly, 4-tab mobile nav + More sheet, privacy toggle, no horiz scroll @390px, quote math canonical vector $1,150–$1,250/deposit $312.50 intact.
- Baseline quote totals recorded for Step 2 verification: A(2BR,3crew,5h,truck,30mi,0fl)=low1150/high1250/dep312.50 · B(3BR,4crew,6h min,truck,45mi,2fl)=low1900/high2100/dep525 · C(labor-only,2crew,3h,0mi,piano-upright)=low1000/high1100/dep275
- Existing rates keys (settings/rates): manHour 65, cushionPercent 10, travelTruck 125, travelLabor 75, mileageAllowance 20, overageRate .85, stairFlight 85, packingRate 65, depositPercent 25, roundingIncrement 50
- Steps remaining: 2 (settings pricing keys), 3 (scope calculator owner-only from haul-yeah-job-calculator-v2.jsx), 4 (lead_scopes collection), 5 (access tiers + assignment control), 6 (video survey fields), 7 (full verify + Save to GitHub)
- Pre-existing issue noted by tester (NOT touched per rules): MetaCard.jsx L131 <Badge> inside <p> hydration warning; Ops brain "Owed to you" vs KPI "Owed to us" disagreement (per CHANGES.md, pre-existing data question)

## 2026-08-21 — Step 2 DONE: Scope pricing settings (owner-only)
- New endpoints GET/PUT /api/settings/scope-pricing (require_owner). Values stored under nested key `scope_pricing` in existing settings doc _id=calculator_rates (old GET /settings/rates response shape untouched — verified same 11 keys). Lazy-seeded on first read.
- Seeded: manHourRate 65, cushionPercent 10, tripFeeTruck 125, tripFeeLabor 75, floorTruck 650, floorLabor 375, roundingIncrement 25, depositPercent 25, manHoursPer100CuFt 2.1
- Settings.jsx: new ScopePricingCard (testids scope-{key}-input, scope-pricing-save-btn), own save flow. /settings route is owner-only.
- Verified: sales GET/PUT → 403, unauth → 401; owner PUT round-trip + per-key stamps; 3 baseline quote totals IDENTICAL before/after (A 1150/1250/312.50, B 1900/2100/525, C 1000/1100/275); build exit 0; quote-calc + auth pytest 40 passed.
- Next: Step 3 (scope calculator page from haul-yeah-job-calculator-v2.jsx, owner-only) — awaiting user go-ahead.

## 2026-08-21 — Step 3 DONE: Job Scope Calculator (owner-only)
- New page /scope-calculator (pages/ScopeCalculator.jsx, default export ScopeCalculator). Scope arithmetic from v2 file byte-faithful (ROOMS/ITEMS/PACKING/ACCESS/MATERIALS/TRUCK_CF/TRUCK_LBS/crew sizing/billable-hour floor). Final pricing steps per spec, all from settings scope-pricing: labor=billMH×manHourRate; travel=tripFee×trucks (jobType-dependent); subtotal; ×(1+cushion); max(floor); ceil to roundingIncrement (never down); deposit=total×depositPct.
- Job type selector truck/labor (default truck) — swaps trip fee AND floor. Output modes: Range (band via ±1 tier shift on high-variance, non-binding) and Final (single firm price+deposit) hard-gated behind Mark Survey Complete.
- Saved scopes: lead_scopes collection (POST/GET /api/scopes, require_owner for now), client sends pricing snapshot; reopening uses stored snapshot (Settings changes never re-price). Restyled to design tokens (no hardcoded colors/fonts/inline style block); steppers 22px visual with 44px hit area (::before -inset-11px).
- Nav: Layout NAV entry (owner, Sales group, Boxes icon); route only in owner branch of App.js.
- Verified: roundUp(641)=650, floor clamps exactly at 375/650 when cushioned below (note: engine's 6-mh billable floor means min real labor-only job = $525; price floor binds only when cushioned<375), jobType switches fee+floor, snapshot survives manHourRate 65→80→65 change, sales/crew API 403 + URL redirect, quote-calc canonical vector intact, build exit 0, testing agent iteration_24 100% (10/10 scenarios).
- Next: Step 4 (scopes on the lead: list/refine/start-new) — awaiting user.

## 2026-08-21 — Step 4 DONE: Scopes on the lead (refine / start new)
- Backend: refined_from field added to lead_scopes docs (ScopeSavePayload). No other collection touched.
- New components/LeadScopesCard.jsx on LeadDetail (owner-gated for now): lists lead's scopes newest-first with creator/date/price, "Refine this scope" per row + "Start a new scope" → /scope-calculator?lead=&name=[&refine=].
- ScopeCalculator: reads query params; refine mode prefills inputs at LIVE pricing with banner + "Save as new version"; snapshot-view hides save card and offers "Refine this scope"; save includes lead_id + refined_from. New testids: scope-refine-banner, scope-cancel-refine-btn, scope-refine-saved-btn, scope-saved-refine-btn, scope-saved-open-btn, lead-scopes-card, lead-scope-row, lead-scope-refine-btn, lead-scope-new-btn, lead-scopes-empty.
- Verified (curl + UI): originals never overwritten across refine & fresh saves; newest-first order; refined_from chains recorded; UI refine flow end-to-end (banner, prefill, save-as-new-version, snapshot banner). Build exit 0, lint clean. users collection fields unchanged.
- NOTE: LeadDetail on-lead card untestable with a real lead in preview (Airtable key missing) — verified via direct URL param flow instead.
- Next: Step 5 (access tiers + Crew-page assignment control + backend enforcement) — awaiting user.

## 2026-08-21 — Step 5 DONE: Access tiers + assignment control + backend enforcement
- Tier resolution (scope_tier_for): owner role → owner; users.calculator_access ("survey"/"final") → that; sales role/roles → sales; else none.
- New endpoints: GET /api/scopes/access (tier), GET /api/scopes/pricing-values (403 for survey/none), PUT/DELETE /api/users/{id}/calculator-access + GET /api/users/calculator-access (owner-only, audit-logged via existing audit()).
- POST/GET /api/scopes now tier-gated with redact_scope_doc: sales loses finalTotal/deposit/pricing; survey loses ALL monetary + pricing (server injects snapshot for owner's benefit); final full minus margin (margin never stored/served). Final-quote saves + survey_complete rejected per tier (403).
- Frontend: ScopeCalculator tier gates (no-access page, survey no-pricing card + Submit to owner, sales range-only: no mode buttons/survey toggle/price build/man-hours/weight/calibration/margin; final = owner minus margin+calibration). Layout: static nav owner+sales, dynamic for assigned crew/marketing. Routes added to sales/crew/marketing branches. TeamTab CalcAccessControl (grant dialog w/ Confirm, immediate 2-way toggle, ✕ w/ confirm; hidden on owner rows). LeadScopesCard now owner+sales.
- users doc: NEW optional field calculator_access only. Login flow untouched.
- Verified: 36/36 direct-API enforcement tests (script /tmp/test_tiers.py, all tiers, immediate toggle/removal, audit trail 3 entries); testing agent iteration_25 8/9 → survey $-leak in Items/Access/Materials labels fixed + retested (0 dollar strings); build exit 0; crew/team/auth pytest 98 passed (1 known parallel flake passes isolated).
- Next: Step 6 (video survey fields on lead_scopes; video counts as completed survey) — awaiting user.

## 2026-08-21 — Step 6 DONE + crew/hours overrides (user add-on)
- Video survey: lead_scopes docs get video {link, received, received_date} (sanitized). New PATCH /api/scopes/{id}/video (any tier w/ access). Calculator: Video survey block (all tiers, testids scope-video-link-input/-received-checkbox/-date-input); LeadScopesCard: per-row "video ✓" + editable VideoSurveyBlock on latest scope (lead-video-* testids). Received video counts as completed survey → unlocks Final (surveyDone = surveyComplete || video.received; effect drops final when neither). No upload/object storage/portal code touched.
- Crew & time overrides (ALL tiers): scope-crew-time-card with crew stepper (min1/max12, "recommended N", reset) + hours-on-site input (0.5 steps, reset). Engine: crew = crewOverride || crewRec (floor uses min(crew,4)); hoursOverride → billMH = hours × crew (explicit override bypasses billable floor), onsite = hours. Overrides persist in saved inputs, restore on open/refine, reset on Clear. Quote Calculator already had manual crew/hours inputs — untouched.
- Verified: UI math exact — base $650–$700; hours=4 → $675–$775; crew3+hours4 → $950–$1,075; resets restore; video gate unlocks/locks Final with auto-dated receipt; video API save/PATCH/persist pass; build exit 0; lint 0 errors.
- Next: Step 7 (full end-to-end verification + Save to GitHub reminder) — awaiting user.

## 2026-08-21 — PRICING SPEC v2.0 (Parts A–D) + weekend phone script ✅ tested iteration_27 (100% both)
- **Part A — old Quote Calculator DELETED**: pages/Calculator.jsx, components/QuoteCalculator.jsx, components/QuoteModal.jsx removed; /calculator routes gone (owner catch-all → /, sales → /leads); Layout nav entry + sales bottom tab now /scope-calculator; Leads card Quote btn + LeadDetail lead-detail-quote-btn navigate to /scope-calculator?lead=&name=; lib/pricing.js trimmed (DEFAULT_RATES fallback + depositFromQuote), lib/quote.js trimmed (quoteSmsBody only); api.js dropped getScopePricingApi/saveScopePricingApi/listCalcItemsApi/saveCalcItemsApi; AppContext rates fetch owner-only. Backend /quotes breakdown + PDF endpoints KEPT (history/PDF still work; iter20 pytest passes).
- **Part B — single source of truth**: server.py DEFAULT_RATES rewritten to v2.0 (~45 flat keys stored at settings doc nested `pricing_v2` + `pricing_v2_updated_at`, migrates legacy scope_pricing on first read via get_rates_values()): manHourRate 65, cushionPercent 10, depositPercent 25, roundingIncrement 25 (round UP), tripFeeTruck/Labor 125/75, floorTruck/Labor 650/375, minHoursTruck/Labor 3/2, zones (zone1MaxMiles 20 fee 0, zone2 40/+150, zone3 60/+300, beyond = custom-quote BLOCK), stairFlightFee 80 (per flight EACH end, flat — no volume scaling on billed $), longCarryFee 100/location, disassemblyFee 100/piece, extraStopFee 125, surcharges (upright 500, grand 800, pool 600, safeT1/T2/T3 200/500/800 at <300/300-700/>700 lb, gymT1 150 treadmill/single, gymT2 300 rack/multi, moto 300), materials 15/12/25, hardFloorBedrooms 3 + hardFloorHours 6, package defaults (studio 2/3.5, 2br 3/5.5, 3br 4/6.5, 4br 4/8), serviceStates "NJ" (string key). GET/PUT /settings/rates now OWNER-ONLY, dict payload validated. Old /settings/scope-pricing endpoints REMOVED (404).
- **New engine lib** /app/frontend/src/lib/scopeEngine.js (pure, dependency-free, node-testable): ROOMS/ITEMS/ACCESS/MATERIALS/PKGS data + computeScope/scopeOutputs/zoneInfo/stateGate/roundUpTo. ITEMS: safe split into safe1/2/3 tiers, tread=gymT1, gym=gymT2, all $ via billKey from config (LEGACY_ITEM_KEYS maps old saved qty.safe→safe2). Engine rules: 3BR+ (bedsEff from counted bedrooms OR package) forces crewRec≥4 + HARD 6-hr floor (hoursOverride cannot go below 6×min(crew,4)); 2BR w/ crewRec 3 & schedMH/3>6h auto-switches to 4 movers; labor-only min 2h / truck 3h; zone fee in subtotal pre-cushion; total=max(cushioned, floor); band/final roundUp to $25.
- **ScopeCalculator.jsx v2 UI**: Quick-start package buttons (scope-pkg-{studio|br2|br3|br4}-btn set crew+hours overrides + bedsEff), Distance & service area (scope-miles-input one-way, scope-pickup-state/scope-dropoff-state selects, scope-zone-note), >60mi → scope-custom-quote-block (price hidden, save allowed as mode "custom"), out-of-state (NY/PA/DE/CT/Other vs serviceStates) → scope-out-of-state-block banner + scope-state-refusal-card + save DISABLED, scope-hard-floor-note when 6-hr floor binds. Per-item/access/material $ labels + trip-fee hint moved behind showBuild (owner/final only — sales sees band only). Saved snapshot P merges livePricing for pre-v2 keys.
- **Part C — security (all verified)**: /settings/rates sales/crew/marketing 403, unauth 401; /scopes/pricing-values → sales gets CUSHION-FOLDED values (every $ key ×1.1 via CUSHION_FOLDED_KEYS, cushionPercent 0 — bands price out IDENTICAL to owner, proven TC15) — floors NOT folded; survey 403; scope saves: sales final-mode 403, response redaction strips pricing+finalTotal/deposit/distFee (distFee added to SCOPE_MONETARY_KEYS), snapshots ALWAYS store true server values (owner/final may send client copy; sales/survey get server truth so folded values never poison snapshots). crew/active-job confirmed money-free.
- **Part D — 11+ math test cases**: /tmp/test_pricing_v2.mjs (node, engine copied to .mjs) — 56/56 PASS: roundup 641→650/never down, truck floor 650 + deposit 162.50, labor floor 375 + 2h min, trip fee swap Δ=55 cushioned, zone boundaries 20/21/40/41/60/61, custom >60 adds no fee, NY/PA/DE/Other blocked NJ ok, 3BR hard floor 24mh beats 2h override, 2BR auto-switch to 4 crew at schedMH 19.8, all surcharge amounts, flat accBill 880, cushion=sub×1.1, deposit=25% of rounded final, sales-folded band === owner band.
- **Phone script**: scriptContent.js rewritten to v2.0 weekend script (opening/scope/access/specialty-ask-once/quote+`price holds` line/objections itemize-never-discount/close 25% deposit+72h refund+ACH/not-ready branch + SCRIPT_EXAMPLES 3 sample exchanges + v2 cheat sheet); ScriptPanel renders examples; Script.jsx has script-download-btn → /haul-yeah-phone-script.docx (brand navy/orange, cheat-sheet last page, generated via python-docx into frontend/public — regenerate with a similar script if numbers change). Help.jsx cheat sheet rewritten to v2 values (live from /settings/rates, owner page).
- Bonus: MetaCard.jsx <p><Badge/></p> hydration warning finally fixed (p→div).
- testing_agent iteration_27: backend 6/6, frontend 100% all roles (owner flows, sales band-only parity $1,250–$1,400 2BR pkg, crew/marketing no-access, deleted routes, Settings roundtrip, docx 200). New pytest: /app/backend/tests/test_scope_v2_regression.py (ghost admins land as owner — tests must POST /auth/switch-role).
- NOTE: pricing lives in Mongo `settings._id=calculator_rates` key `pricing_v2`; production redeploy will seed v2 defaults on first read and migrate any stored scope_pricing values.

## 2026-08-22 — Rewards Ecosystem + Haul Points + AI Challenge Agent ✅ tested iteration_28 (100% both, 22/22 pytest)
User approved: reuse Emergent LLM key (GPT), owner UI as Team HQ tabs, 10 pts=$1 default. ALL ADDITIVE — invariants verified: crew rates 28/24 untouched, commission config untouched, 27+ badges / 4 existing challenges / HOF / leaderboards / job credits intact.
- **Reward engine (backend `rewards engine` section ~line 5550)**: `rewards` collection (full record: user/team/challenge/badge refs, reward_type cash|gift_card|points|merch|meal|custom|redemption, status pending→approved→fulfilled +denied/voided, payroll_required + payroll_period, approved/fulfilled_by/at, void_reason). `points_ledger` + cached `users.haul_points`/`haul_points_lifetime` via credit_points() (negative-balance guard). Settings doc `rewards_config` (DEFAULT_REWARDS_CONFIG, 20 keys: pointsPerDollar 10, crew $300 / sales $400 monthly budgets, maxSingleReward $250, enabledRewardTypes, 13 AI settings). Endpoints (owner unless noted): GET/PUT /rewards/config, GET /rewards, POST /rewards/{id}/(approve|deny|fulfill|void) — approve credits points / deducts redemption points, void reverses; GET /rewards/summary (budget committed incl. pending, totals, ai spend), GET /rewards/payroll CSV, catalog CRUD (/rewards/catalog, 9 seeded items), POST /rewards/redeem (any user; points held while pending), GET /rewards/wallet (self or owner), POST /rewards/recognition (badge/title/points/spot-bonus, auto-approved, audit).
- **Challenge extensions**: reward kinds badge|title|cash|gift_card|points|merch|meal|custom|combo (validated vs config caps + enabled types); type "team"; eligible_roles all|driver|helper|dual (driver/helper filter job_credit counting by role_tag); _award_challenge creates PENDING rewards for monetary parts (badges/titles still instant) + celebration notify; create_challenge notifies team members. Badge `unlock_reward` {points, amount} — one-time pending reward on unlock; seeded (only where absent): truck-boss/road-captain/five-star-shoutout/deal-dozen/deposit-locksmith/comeback-kid 250pts; haul-of-famer/two-way-player/quarter-club 500pts. Owner-editable via badge form.
- **AI Challenge Agent (backend `AI challenge agent` section)**: 14 CHALLENGE_TEMPLATES (Safety Scout, Customer Hero, Ready to Roll, Road Captain of the Month, Crew MVP, Team Player, Perfect Prep, Full House, Weekly Deposit Sprint, Consistency King, Comeback Challenge, Monthly Sales Champion, Revenue Milestone, Deal Drive). LLM = emergentintegrations gpt-5.4 (strict JSON schema) grounded in _agent_baselines (30/60/90d job_credits, active members, per-member monthly avg); deterministic template fallback when LLM fails. _guard_draft: AGENT_BANNED regex (speed/injury/damage-hiding/overwork/overcharge), metric whitelist, per-challenge cap aiMaxRewardPerChallenge ($100) + global cap, AI monthly budgets ($200 crew/$250 sales) vs published estimated_cost, duplicate check (template keys + names, 6 months), target sanity ≤2.5× baseline, ≤62-day span. Endpoints: POST /challenge-agent/generate {team,count}, GET drafts, PATCH draft (re-guarded), publish (notifies team, created_by "ai-agent"), reject, status, retry. `challenge_agent_loop` (4h tick, started in startup): idempotent monthly run via `challenge_gen_runs` _id "{YYYY-MM}:auto"; failure → owner "generation failed — retry" notification. Defaults: generate monthly ON, auto-publish OFF, approval required ON.
- **Frontend**: TeamAdmin tabs Rewards (AdminRewardsTab: budget bars, queue tabs w/ approve/deny/fulfill/void, totals, Give Recognition dialog, Reward settings dialog, catalog manager, payroll CSV download) + AI Agent (AdminAgentTab: status card w/ retry banner, generate both/crew/sales, draft preview cards §36-style with why/baseline/cost/confidence + Approve&Publish/Edit/Regenerate/Reject, agent settings toggles+numbers). ChallengeCard: multi-part rewardLabel (exported), eligibility/type/owner-verified/"agent pick" chips, days-left, my % complete. Challenges.jsx: Haul Points chip → profile, celebration dialog on unseen wins (localStorage hy_celebrated_{id}). RewardsWallet.jsx on MemberProfile (can_see_numbers = self/owner): balance/lifetime/cash/pending stats, redemption grid (self, held-points aware), history w/ status chips. AdminChallengesTab: team type, eligibility select, 9 reward kinds w/ conditional inputs. AdminBadgesTab: milestone points/$ inputs + "unlocks X pts" hint.
- Testing agent iteration_28: backend 22/22 pytest (test_iteration28_rewards_agent.py kept in tests/), frontend 100% incl. mobile 390px. Known-low note: voiding a reward whose user was hard-deleted 404s in credit_points (users are deactivated not deleted in normal ops).
- Preview state: 6 AI drafts left waiting in Team HQ → AI Agent as a demo; gen run 2026-08:auto recorded; no pending rewards.



## 2026-08-22 — 8-point fix list ✅ tested iteration_29 (100% both, 422+ pytest green)
User-approved choices: Square = money source of truth; mileage = $0.85 × max(0, one-way miles − 20); shared passwords fully removed; "Chariltong Piercin" spelling is correct (no change); Junior canonical name = "Junior Saintil".
1. LEAD FIELDS: fields.js → isLegacyLeadField()/findAccessFieldIds()/leadAccess() match by field NAME from schema (no hardcoded field IDs — preview lacks Airtable key). Legacy Floor/Stairs/Elevator fields hidden from extras. Pickup/Drop-off access shown first-class on lead card (lead-access-info), lead detail (lead-detail-pickup-access/-dropoff-access), Day Sheet (looks up linked lead via PF.lead), Scope Calculator banner (scope-lead-access) + home-size package prefill for leads.
2. MILEAGE: scopeEngine mileageInfo() replaces zoneInfo; DEFAULT_RATES mileageFreeMiles=20 / mileageRatePerMile=0.85 (folded ×1.1 for sales via CUSHION_FOLDED_KEYS); custom-quote >60mi block REMOVED (every trip prices); get_rates_values() one-time Mongo migration strips zone keys + bumps stairs 80→85 (runs on prod at deploy). Settings/Help/scriptContent updated.
3. STAIRS: $85/flight everywhere (backend default + migration + frontend fallback + phone script example $510).
4. MONEY: GET /api/money/summary (owner-only) = ONE Square-backed source (booked=deposit-backed jobs quote sum; collected=PAID square invoices; outstanding=job remaining vs Square payments + open invoices w/o job; revenue_today). Dashboard THE MONEY card, _ops_facts money, Partner (rewritten), Invoices KPIs all read it. Burn flicker fixed via loading guards ("—" until data).
5. DAY SHEET SYNC: sync_project_for_job() upserts Airtable Projects (finds record by PROJECT_LEAD_LINK_FIELD, never downgrades In Progress/Completed status, never renames existing) on deposit paid + when job_date patched; stores project_record_id/project_synced_date on jobs to avoid re-sync storms at startup backfill. Preview: skipped w/ 503 warning (no Airtable key) — VERIFY ON PRODUCTION after deploy.
6. AUTH: _login_shared_password + ROLE_ENV + ROLES deleted; login requires email → 401 "Shared passwords are retired…" otherwise. AuthGate requires both fields. All pytest login helpers moved to email logins (ghost testsalesadmin/testcrewadmin for role tests; owner-switch token for employee-role tests). APP_PASSWORD env kept ONLY for owner user seeding.
7. HYGIENE: dedupe_crew_users() startup migration merges dup users by name (javante brown; junior saintil/santil → "Junior Saintil"), re-points time_entries/job_credits/availability/points_ledger/rewards/notifications/assignments.crew/user_badges, sums haul points, deletes dup. Runs on prod at next deploy.
8. SECURITY: no regressions — iteration_29 re-verified 403s (sales/crew blocked from owner tables), money/summary owner-only.
New regression test: /app/backend/tests/test_iteration29_review.py. Report: /app/test_reports/iteration_29.json (100%/100%).
NEXT/backlog: Phase E route optimization (P1), CAPI test button (P2), ETA texts (P2), AI estimate PDF (P2), weekly digest text (P2). Reminder to user: Save to GitHub + redeploy so migrations (rates, crew dedupe) run on production.

## 2026-08-28 — Specialty Item Engine ✅ tested (28/28 engine acceptance T1–T10, pytest 440/0, iteration_30 frontend 11/11)
Additive, non-breaking (stairFlightFee 85, mileage model, computeScope household arithmetic untouched — T1 anchors bit-identical).
- 8 new rate keys in all four places (server DEFAULT_RATES, pricing.js mirror, NO_PRICING=0, CUSHION_FOLDED_KEYS gets ONLY the 4 surchargeCustomB* dollars, never the ratios): surchargeCustomB1 75 / B2 150 / B3 250 / B4 400, customBuiltInMultiplier 1.25, customDisconnectMultiplier 1.15, customSwapFactor 0.60, specialtyHandlingCapPct 30. Owner-editable in Settings → 'Custom specialty items'.
- scopeEngine.js: CUSTOM_BANDS (weight-banded, w800plus = hard block); inputs.custom[] (defaults [] for old scopes — T10); custom items feed the SAME cf/lbs/itemMH accumulators; specialtyOnly DERIVED (no rooms + any item); handlingBilled = min(raw, labor×capPct) only when specialtyOnly (household bit-identical — T2); crewFloor/crewFloorStairs beats crewOverride (T7); blockers[] for clearance (width+2>path) and 800+ (T5/T6). handlingRaw/handlingBilled returned live but NOT persisted (SCOPE_MONETARY_KEYS/redact untouched — saved result only gains non-monetary specialtyOnly + blockers).
- ScopeCalculator.jsx: custom items card (scope-custom-*), derived mode indicator (scope-mode-indicator + scope-specialty-prompt), blocker card replaces quote + locks Final (scope-blocker-card), cap note (scope-handling-cap-note), crew-floor note, owner-only client-side market sanity card (SANITY_BANDS live in the JSX only, never saved — scope-sanity-card/-select/-warning), disconnect operator note, tape-measure hint.
- Tests: /app/frontend/scripts/specialty-engine-tests.mjs (T1–T8, T10; anchors_before.json = pre-change anchors) + /app/backend/tests/test_specialty_rates.py (T9: sales folds B4→440, ratios unfolded). Full suite green (440 passed).
- NOTE for future agents: pytest commission + availability tests are STATE-SENSITIVE — leftover recQATEST* lead_commissions docs or QA availability docs from an interrupted run cause false failures; prune them before blaming code.

## 2026-08-28 — Fit-work follow-up (install/uninstall separated from handling) ✅ 40/40 engine tests, pytest 440/0, UI verified live
- CUSTOM_BANDS mh now PURE HANDLING: under150 0.5 (unchanged), w150_299 1.0, w300_499 1.5, w500_799 2.0. cf/lbs/crewFloors/billKeys untouched.
- New per-band removeMh/placeMh (0.25/0.25, 0.5/0.5, 0.75/1.0, 1.0/1.5 — placement > removal) + FIT_WORK_OPTIONS export.
- New custom-item field fitWork (none|removeOnly|placeOnly|both, defaults "none"; missing key reads as none — old scopes unchanged). fitMh adds to itemMH per unit × swap units; engine returns fitMH. TIME ONLY — no rate key, no dollar line, doesn't touch handling cap.
- UI: "Fitting work" select (scope-custom-fitwork-select) + live "+X.XXmh each — billed as time, not a fee" (scope-custom-fitwork-hours) + "Ask directly..." helper.
- Reference actuals: Case A (2× w500_799 built-in, one swap, stairs 1+1, labor, fitWork none): was $1,025/crew4/2.63h onsite → now $1,200/crew4/3.20h (billMH 9.78, labor 635.70, cap 190.71 exact). Case B (fitWork both): $1,900/crew4/5.07h (fitMH 7.5, billMH 17.28, labor 1123.20, cap 336.96 exact, handlingRaw identical 1300 A vs B). Household anchors byte-identical (T1 3/3).
- Tests: F1–F7 appended to /app/frontend/scripts/specialty-engine-tests.mjs (40/40); /app/frontend/scripts/fitwork-cases.mjs prints the reference table. Playwright now installed in pod; ad-hoc UI check at /app/test_reports/fitwork_ui_check.py.

## 2026-06 (June) — Scope Calculator v2.1 DEPLOY 1 (P0 only) ✅ engine 52/52 + 40/40, pytest 25/25, testing_agent iteration_31 100% (8/8 frontend)
Spec: /app/memory/v2.1_prompt.md. Shipped P0 ONLY; P1 (commercial mode, payload-overage choice, long-distance, capacity/multi-day) and P2 (clearance-blank prompt, published-price flag, deep-link session P2-3, stepper rapid-click, mobile 390 audit) + the calibration loop are Deploy 2, intentionally NOT built.
- **P0-1** removed `crew = 3 × trucks` (scopeEngine.js) — truck count never sets crew again. Added day-length escalation: when schedMH/crewRec > maxHoursOnSite(10), crew rises to round(schedMH/targetHoursOnSite 8), capped at maxCrewPerDay(12). Escalation never changes price (crew only affects the floor via min(crew,4), which never binds on jobs big enough to escalate). N1 3BR+piano now crew 4 (was 6); N2 80-mh POD escalates to ~10.
- **P0-2** labor-only never computes trucks: split `trucksRaw` into `displayTrucks` (0 on labor → truck SVG + banner hidden, scope-labor-no-truck note shown) and `billableTrips` (1 on labor → trip fee ×1, still $75). travel = tripFee × billableTrips. N2 labor POD → no banner, trip $75 (was ×3/×4).
- **P0-3** specialty-only ignores the package ENTIRELY: engine sets bedsEff=0 and drops the package's crewOverride/hoursOverride (effCrewOverride/effHoursOverride) when specialtyOnly && pkg; UI also clears the package via effect (so manual overrides work again) + scope-specialty-prompt 'package sizing ignored'. N3 one treadmill (w300_499) + 3BR pkg + labor → crew 3, billMH 6, labor $390, handling raw $250→billed $117 (30% cap), FINAL $650 (was the $2,075 catastrophe; $850 = the half-fix trap, both avoided).
- **P0-4** assessment-required scopes are savable: Save button no longer disabled when r.blockers.length>0 (was gated by r.cf===0); label 'Save for assessment'; saveScope nulls ALL money (bandLo/Hi/finalTotal/deposit/distFee) and stores result.mode='assessment_required'; saved-list + LeadScopesCard show 'assessment' / 'On-site assessment required' (never a dollar). Price suppression on the 800lb+ blocker unchanged.
- **P0-5** mileageRatePerMile seed default 0.85 → **6.00** in backend DEFAULT_RATES + frontend pricing.js (fixes the latent fallback underpricing bug; prod already 6.00 live). Preview stored value aligned to 6.00 via PUT. The 3 stale-0.85 test assertions (test_scope_v2_regression.py, test_iteration29_review.py ×2) now READ the live rate from the endpoint (sales-folded derived as owner×(1+cushion)) so a future pricing change won't break the suite. N5 150mi → mileage line $780 (130 × $6.00).
- **New settings (owner-editable):** maxCrewPerDay 12, targetHoursOnSite 8, maxHoursOnSite 10 — added to backend DEFAULT_RATES, pricing.js, scopeEngine NO_PRICING (real values, non-monetary), and Settings.jsx new 'Capacity & scheduling' group (scope-maxCrewPerDay/-targetHoursOnSite/-maxHoursOnSite-input). NOT in CUSHION_FOLDED_KEYS (they're crew/hours, not dollars).
- Household anchors byte-identical (T1 A/B/C unchanged to the cent); R1–R5 regression + N1/N2/N3/N5 all green. New acceptance script /app/frontend/scripts/scope-v2.1-deploy1-cases.mjs (52/52). Sales tier verified: range-only, no build/margin/weight leak, band brackets owner firm price.
- DEPLOY 2 BACKLOG (from v2.1_prompt.md, priority order): P1-1 payload overage warn+choose (volume=hard 1-truck-2-trips vs 2-trucks, weight=soft default 1 truck — this is what still makes N1 price $3,325 in Deploy 1 vs the $3,175 target), P1-2 commercial mode (raise room cap 6→50 + numeric input, 8 commercial line items, COI hard gate, separate mh/100cf calibration), P1-3 long-distance (≥50mi banner + round-trip drive hrs + $2,000 day minimum), P1-4 capacity guardrails (trucks/crew vs fleet), P1-5 multi-day split, P2-1..P2-5 quality/bug items, and the calibration loop (log actual mh ÷ cu ft on job completion). Reminder: user must Save to GitHub + redeploy so the 6.00 seed default + new capacity keys seed on production (prod mileage already 6.00 live).

## 2026-06 (June) — Quality & Compliance module (PROMPT 1 of 3) ✅ backend pytest 9/9 (tests/test_quality_module.py), frontend testing_agent iteration_32 100%
Spec: user's "Quality & Compliance module (prompt 1 of 3)", 11 rules. Airtable base appFAHTRNrRckuxI8. NOTE: preview has NO AIRTABLE_API_KEY, so all Airtable-backed flows (queue/register/NC/audit CRUD, quote-accuracy rows) are PROD-ONLY; verified structure, RBAC, and the Mongo-backed parts (cost-stripped quote allowlist, feedback log) e2e in preview.
- **New role `quality`** in server.py: added to ALL_ROLES + SWITCH_ROLES; ROLE_TABLES["quality"]={projects,contacts,tasks,blog,quality_docs,nonconformances,job_audits}; TASK_GROUP_FOR_ROLE["quality"]="crew"; BLOCKED_FIELDS[("quality","projects")]={PROJECT_REVENUE_FIELD} (quality SEES quote, never revenue). Ghost account **TestQualityAdmin / HaulYeah2026!** (email testqualityadmin, seeded in seed_on_startup). Did NOT touch audit()/audit_log (system activity log) — this module is job_audit/job_audits.
- **3 new Airtable tables** in TABLES: quality_docs tblpsrrkQlfa7BVW5, nonconformances tblDYbrboZq9ClrHM, job_audits tblVFJSZCgogIpz4V. All field IDs constants live in server.py (QD_*/NC_*/JA_* near DRIVER_RATE) and frontend lib/quality.js (QD/NC/JA maps + select options). Projects quoted_by fldAew7uXk5JvBqBw + surveyor fldFJUFcOLLRWvc60 (existing, wired). NEVER write AUTONUMBER/FORMULA: NC number, Audit number, Audit due. "Variance explained" opt 1 has an EM DASH: "Yes — customer agreed".
- **Rule 8 cost-stripped quote (Mongo, testable):** GET /api/quotes/{lead_id} now allows quality; `_strip_quote_for_quality` is an ALLOWLIST (`_QUALITY_QUOTE_ALLOWLIST`) — copies only customer-facing keys (customerName/phone, jobDate, addresses, package, crewSize, estimatedHours, hourlyRate, tripFee, mileage*, distFee, stairs/longCarry/specialty/extraStop/materials, subtotal, finalQuote/finalTotal, quoteLow/High, bandLo/Hi, deposit, invoiceLineItems/lineItems/surcharges) and DROPS everything else (fails closed). PUT /quotes untouched. Verified none of cushion/crewCost/margin/commission/cost leak; owner still sees full.
- **Backend endpoints (owner+quality):** GET /api/quality/audit-queue (Completed projects with NO linked job_audit + days_since + audit_due=move_date+7 + past_due, Airtable), GET /api/quality/quote-accuracy (per-Completed-job quoted vs final [final from linked audit], est [PROJECT_HOURS_FIELD] vs actual [time_entries on-site span via Mongo jobs->job_date->assignments], hours flag amber>0.5/red>1.0, over_max flag vs rates.maxHoursOnSite, summary mean+per-rep; filters from/to/rep). POST/GET /api/quality/feedback (Mongo `quality_feedback` + writes NC Type "Quote variance"/Severity "Minor"; owner+quality create; GET: owner/quality all, a sales rep sees ONLY feedback where rep_user_id==their id; notifies rep). Auto-NC hook in create_record for job_audits: Result "Pass with findings"/"Fail" opens linked NC Type "Audit finding" + links audit back. Close-guard in update_record for nonconformances: Closed requires Root cause + Corrective action (422 otherwise).
- **Frontend:** App.js `role==="quality"` branch (lands /quality/audits) + 4 routes added to owner branch. Layout NAV "Quality" group (owner+quality: Job Audits/Nonconformances/Documents/Quote Accuracy) + PRIMARY_TABS.quality + GROUP_ORDER + ROLE_LABEL. Pages: QualityAudits.jsx (queue, past-due red, audit form w/ BOL chips), QualityProblems.jsx (NC log, Status/Severity filters, close-validation), QualityDocuments.jsx (register, Drive URLs only NO upload, review<=60d amber/overdue red), QualityQuoteAccuracy.jsx (filters, summary strip, per-rep, expandable rows w/ cost-stripped breakdown beside actuals + per-job feedback form; feedback log ALWAYS renders even when Airtable-missing). lib/api.js: quality* helpers. data-testids throughout.
- Tests: /app/backend/tests/test_quality_module.py (9/9). Report: /app/test_reports/iteration_32.json (frontend 100%).
- DO NOT BUILD (prompts 2 & 3): compliance gates, claims tracking, KPI dashboards, review tracking.
- REMINDER to user: Save to GitHub + redeploy so the quality role, TestQualityAdmin seed, and 3-table wiring reach production (Airtable data flows are live only in prod).

## 2026-06 (June) — Quality & Compliance module (PROMPT 2 of 3) ✅ backend pytest 40/40 (tests/test_quality_module.py), frontend wiring testing_agent iteration_33 100% (4 roles)
Spec: user's "prompt 2 of 3" — 14 compliance gates on the Projects detail view, owner-only override, quality read-only. Airtable base appFAHTRNrRckuxI8 (preview has NO AIRTABLE_API_KEY → the panel/override dialog render ONLY on production; the gate LOGIC is pure and fully tested in preview). Prompt 3 (claims/KPIs/reviews) NOT built.
- **Gate evaluator `evaluate_gates(pr, ctx)` (server.py ~L3378)**: pure function → 14 gates [{key,label,severity,passed,reason,exempt}], sorted by severity. 3 "Liability risk" gates ONLY: brochure_delivered, ofs_24h, owner_operator (GATE_DEFS ~L3245; count asserted). Severities: Liability risk > Regulatory > Major > Minor. Office Goods Only exempts ONLY estimate_24h + ofs_24h (nothing else). 3 gates are RISK-TRIGGERED (owner_operator/labor_equip_change/long_haul_review) and correctly PASS when their risk is absent — a blank job fails the 11 evidence gates, not those 3 (documented in the test; this is intentional, not a bug — false alarms would train reps to ignore the panel). ofs_24h passes on short_notice_proof even if OFS late/missing. long_haul needs owner ack only at ≥60mi. Option-2 protection needs BOTH declared value + deductible.
- **Data split (per user spec):** gate INPUTS live in Airtable projects (17 fields, COMPLIANCE_INPUT_FIELDS map, field IDs ~L96); gate RESULTS/cache + override metadata in Mongo `job_compliance` (_id=project_id, gate_results/summary/evaluated_at/override); a successful override ALSO writes a durable Airtable `nonconformances` record (Type "Gate override", severity = worst failing gate, RaisedBy/Date, What carries the reason, LinkedJob=[project_id]). Override FAILS (502) if the NC write fails — no override survives on a Mongo record alone. Failed gates STAY red after override (nothing turns green).
- **Endpoints:** GET /api/jobs/{id}/compliance (owner/sales/quality; returns inputs+gates+summary+override+can_edit/can_override), PUT (owner/sales only; writes inputs to Airtable + re-evaluates; long_haul_owner_ack is OWNER-ONLY via OWNER_ONLY_INPUTS → 403 for everyone else), POST .../override (require_owner; 20-char reason + typed "OVERRIDE" or 422). Quality role: 403 on any projects PATCH (update_record L2793) and on PUT compliance.
- **Frontend:** components/ComplianceSection.jsx wired into Projects.jsx ProjectCard (expanded Details, ~L205) with canEdit={owner|sales}, canOverride={owner}. App.js: /projects route added to sales + quality branches. api.js: getJobComplianceApi/saveJobComplianceApi/overrideJobComplianceApi.
- **WORDING (user-requested, this session):** (1) gate failure `reason`s rewritten to name the FIX (verbatim Quality-Manual strings for brochure/ofs/owner-operator/protection: e.g. brochure fail = "Send the brochure and record the date. Without it the $1.00/lb cap may not hold."). (2) Override dialog shows the verbatim consequence block (data-testid override-consequence: "An override does not make this job compliant. …logged against your name.") + a liability-only line (data-testid override-liability-warning, gated to hasLiability: "This gate protects our $1.00-per-pound limit of liability. …replacement-value damages on a claim.").
- **Tests:** tests/test_quality_module.py expanded to 40 tests (16 required assertions #1–#16: blank-job, exactly-3-liability, per-gate flip ×14, ofs short-notice, long-haul ack, option-2, office exemption scope, override 403 per role, reason/OVERRIDE validation, NC creation+severity, NC-fail→override-fail, stays-red, quality write 403s, long-haul owner-only). Gate/exemption tests are PURE (import server, mocked ctx, no live base). Override success/fail/stays-red run in-process with server.airtable_request monkeypatched + a persistent asyncio loop (asyncio.run closes the loop between calls → Motor 'Event loop is closed'; reuse one loop). 403/422 permission tests run over HTTP (short-circuit before Airtable). Report: /app/test_reports/iteration_33.json.
- Nuance flagged to user: assertions #1 and #8 as literally worded ("empty job fails all 14"; office job "still FAILS … when inputs empty") were implemented to correct domain semantics — the 3 risk-triggered gates pass when no risk is present, so #8 puts protection/owner-op/labor into their FAILING state on an office job to prove the exemption never widens to them.
- REMINDER to user: Save to GitHub + redeploy so the compliance panel renders against live Airtable projects; the panel + override dialog are PROD-verifiable only (preview has no Airtable key → empty projects list → panel never mounts).

## 2026-06 (June) — Quality & Compliance module (PROMPT 3 of 3) ✅ backend pytest 60/60 (tests/test_quality_module.py, +20 new P3 cases), frontend testing_agent iteration_34 100% of critical criteria
Spec: user's "prompt 3 of 3" — claims tracker, review capture, KPI dashboard, monthly review, quote-accuracy extensions. Airtable base appFAHTRNrRckuxI8. PREVIEW HAS NO AIRTABLE_API_KEY → all Airtable-backed reads return 503 gracefully (pages render shell + error banner, no crash). The ONLY preview-live flows: GET /quality/monthly?month=<past, no snapshot> (200 no_snapshot) and the Mongo-backed Google-review toggle. Everything Airtable-backed is PROD-ONLY — user will walk those screens against a real claim record after deploy.
- **Backend (all injected into server.py; pyflakes clean):**
  - CLAIMS: Airtable table `claims` tblsdVcVP6uhtxOrK, field-id constants CLAIM_* (~L164). `Form due` + `Settlement due` are Airtable FORMULA fields — READ verbatim, NEVER computed/written; the +30-day extension is ALREADY inside the settlement formula (120, never 150). CLAIM_WRITABLE excludes both formulas + autonumber. GET/POST/PATCH /quality/claims (owner+quality). Creating a claim opens the linked "Claim" nonconformance FIRST (severity **Liability risk**, NOT Major) — if the NC write fails the claim is not created (502). `_enforce_claim_owner_rules`: amount_settled + Status "Settled" are OWNER-ONLY (403 per role otherwise). List sorted nearest-deadline (red pinned).
  - REVIEW CAPTURE (Mongo `project_reviews` keyed by project id): `_mark_review_requested` (source observed) auto-runs on project→Completed; `_backfill_reviews` (source backfill, from move date) for jobs flipped Completed straight in Airtable. Portal review ≤4★ hook (portal_review) auto-opens "Low review" NC (Minor). PUT /quality/projects/{id}/google-review toggle. GET /quality/reviews = daily chase list (this month's completed jobs, oldest-first, state none|waiting|received, two-ask flags day2/day6, nothing flagged past day 8).
  - DASHBOARD GET /quality/dashboard (current month live): 5 KPIs (review capture, quote variance last-20, claim rate/100, on-time start, first-time-right) + document_compliance rendered SEPARATELY as a risk measure (11 legal gates = Liability risk + Regulatory; exempt=pass, override=fail; target 100%) + needs_you_now strip (claims ≤14d, overdue audits, open Liability-risk NCs).
  - MONTHLY GET /quality/monthly?month= : past months read the FROZEN Mongo `monthly_metrics` snapshot ONLY — never recompute; a month with no snapshot returns {no_snapshot:true} (never back-fills). Current month = live, labelled in-progress. Month-over-month movement reads prev month's frozen snapshot only. Also lists open NCs, claims ≤14d, overdue audits, crew docs expiring ≤60d. `_snapshot_month` frozen by cron POST /cron/monthly-metrics-snapshot (WEBHOOK_CRON_SECRET) at month rollover.
  - QUOTE-ACCURACY extension: per-rep rollup gated at 5+ completed jobs (below → job count, no mean); SOP-9 package-overrun alert = 3 CONSECUTIVE (by move date) same-package jobs over on hours (alert only, NEVER an NC — pricing signal, not a quality failure).
- **Frontend:** pages QualityDashboard.jsx (/quality, landing for quality role), QualityClaims.jsx (/quality/claims), QualityMonthly.jsx (/quality/monthly); extended QualityQuoteAccuracy.jsx (qa-package-alert banner w/ verbatim SOP-9 wording + linked jobs; qa-rep-insufficient message) and QualityAudits.jsx (inline audit-review-toggle-<id> per row). api.js: qualityDashboardApi/qualityMonthlyApi/qualityReviewsApi/qualityClaimsApi/qualityCreateClaimApi/qualityUpdateClaimApi/qualitySetGoogleReviewApi. lib/quality.js: stateTone/stateBorder/stateDot/metricValue helpers. App.js: routes added to quality + owner branches (quality lands /quality). Layout.jsx: Quality nav = EXACTLY 6 entries per user (Dashboard, Job Audits, Nonconformances, Claims, Documents, Monthly Review) — Quote Accuracy intentionally NOT a nav item; reached as a drill-down from the Dashboard "Quote variance" KPI (kpi-quote-accuracy-link). Review chase toggle also lives on the Dashboard (both surfaces write the same google_review_received field). Owner-only gating visible in claim form (claim-amount-settled disabled + no "Settled" status for quality).
- **Tests:** tests/test_quality_module.py 40→60. The 7 required P3 assertions (Airtable MOCKED, same discipline as prompt 2): (1) settlement_due read verbatim = Completed+120 not 150; (2) form_due/settlement_due/autonumber never written (422 if passed); (3) amount_settled 403 per role quality/sales/employee/crew; (4) Status "Settled" 403 per role; (5) create-claim opens Liability-risk (not Major) linked NC — full endpoint in-process w/ airtable_request+current_principal monkeypatched, persistent loop; (6) CRITICAL_GATE_KEYS == 11 legal gates, exempt=pass via evaluate_gates, override=fail via inspect; (7) no-snapshot month returns no_snapshot over HTTP. Plus RBAC 403s for dashboard/reviews/claims/monthly/google-review. Report: /app/test_reports/iteration_34.json.
- REMINDER to user: Save to GitHub + redeploy so the claims table wiring, review hooks, dashboard/monthly, and the monthly-snapshot cron reach production. Then walk the Airtable-backed screens against a real claim record. Preview-untested (prod-only): claims CRUD, dashboard KPIs, quote-accuracy rows, audit queue, current-month monthly — all 503 in preview without the Airtable key.

## 2026-09-26 — Owner can assign the Quality role via Team Management ✅ self-tested (curl 8 flows + UI screenshot)
Additive only; the quality role already existed everywhere (SWITCH_ROLES, ALL_ROLES, all /quality endpoints, cost-stripped quote allowlist, TestQualityAdmin ghost). The ONLY gap was that user-management CRUD rejected it.
- **Backend server.py:** added "quality" to the allowed-roles tuple in `create_user` (L5131), `patch_user` roles-list (L5173) and single-role (L5181); error strings updated to "…marketing, or quality." No other logic touched — no team, no badges, no challenges for quality (per user choice).
- **Frontend TeamTab.jsx:** `RoleChecks` list now `["crew","sales","marketing","quality","owner"]` (drives BOTH the Add-person and Edit-roles dialogs); `ROLE_BADGE.quality` = violet (`bg-violet-500/12 text-violet-700 border-violet-500/40`) per user choice.
- **Verified (curl against preview):** create quality-only user → login routes to role=quality; create dual sales+quality → POST /auth/switch-role to quality works (can_switch true); PATCH remove quality (→[sales]) and add back (→[sales,quality]); invalid role → 422; quality user hits /quality/dashboard (503 = Airtable-missing, i.e. permission gate PASSED, not 403) and is 403 on owner-only /api/users. UI screenshot confirms the Quality checkbox renders. Test users deleted afterward (roster = owner only).
- NOTE: not run through testing_agent (5-line additive enum change, fully curl-verified). No new persistent credentials (test_credentials.md already documents TestQualityAdmin ghost).
- REMINDER to user: Save to GitHub + redeploy so the role-assignment change reaches production.
